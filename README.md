# Nachweis landing page

Static pitch site for Nachweis (ETHOnline 2026). Plain HTML, CSS and vanilla JS, self-hosted fonts, no build step.

Serve locally:

    python3 -m http.server 8787 --bind 127.0.0.1

The first draft is kept under v1/ for comparison. Domain: nachweis.dev (pending).
